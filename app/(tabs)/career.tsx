// Вкладка «Карьера» — постоянная работа. Экран живёт в feed.tsx рядом со
// сменами (общие карточки и логика колоды), здесь только точка входа роутера.
import { CareerScreen } from './feed';

export default CareerScreen;
