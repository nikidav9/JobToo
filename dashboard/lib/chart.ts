/**
 * Оформление графиков — одно на всю панель.
 *
 * Раньше эти же четыре константы были скопированы в десять файлов, и копии
 * успели разойтись: где-то тень `md`, где-то `sm`. Расхождение мелкое, но
 * именно из таких мелочей складывается ощущение, что разделы делали разные
 * люди в разное время.
 *
 * Цвета осей и сетки заданы в токенах, а не в hex: раньше здесь стояли
 * кремовые `#9A9690` и `#E8E6DF` — остатки прошлой палитры, из-за которых
 * графики оставались тёплыми на холодном фоне.
 *
 * Recharts не понимает `var(...)` в SVG-атрибутах вроде `stroke`, поэтому
 * для них есть готовые значения — те же, что и в токенах.
 */

/** Всплывающая подсказка. */
export const TT = {
  borderRadius: 8,
  border: '1px solid var(--line)',
  background: 'var(--bg-elev)',
  color: 'var(--ink)',
  fontSize: 12,
  boxShadow: 'var(--shadow-md)',
}

/** Подписи осей: служебные, поэтому `--ink-3`. */
export const AXIS = {
  fontSize: 10,
  fill: '#5E6875',
  fontFamily: 'Geist Mono, monospace',
}

/** Ось категорий читается как текст, а не как разметка — отсюда `--ink-2`. */
export const AXIS_CAT = { ...AXIS, fill: '#414A57' }

/** Сетка. Заметна ровно настолько, чтобы по ней можно было вести взгляд. */
export const GRID = '#E1E5EB'

/** Подписи под графиком. */
export const LEGEND = { fontSize: 12, color: '#5E6875' }
