<?php
// Образец. На хостинге рядом с db.php и tg.php должен лежать файл
// sb_service_key.php ровно такого вида, но с настоящим ключом внутри.
//
// Где взять ключ: Supabase → Settings → API → Project API keys → service_role.
// Это ключ администратора: он обходит RLS и видит всю базу. Поэтому он
// живёт только на сервере — ни в приложении, ни в дашборде, ни в git.
//
// Файл в репозиторий не коммитим (см. .gitignore).

return 'ВСТАВЬТЕ_СЮДА_SERVICE_ROLE_КЛЮЧ';
