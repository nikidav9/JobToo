-- Нажатие на уведомление в колокольчике должно открывать нужный экран.
-- Для этого рядом с текстом храним вид уведомления и его цель (id чата и т. п.).
-- Колонки необязательные: старые записи останутся без них, приложение это
-- переживает и в таком случае определяет экран по заголовку.

alter table jm_notifications
  add column if not exists type text,
  add column if not exists payload jsonb;
