-- Закрываем базу от публичного ключа.
--
-- Сейчас RLS выключены, а анонимный ключ имеет полный доступ к таблицам.
-- Ключ этот публичный по своей природе — он лежит в бандле сайта jobtoo.ru
-- и в мобильном приложении. То есть прочитать телефоны, имена и все
-- переписки может любой, кто открыл сайт и заглянул в исходники страницы.
--
-- Приложение к таблицам напрямую не ходит: весь обмен идёт через
-- php-proxy/db.php. Поэтому анонимный доступ можно закрыть целиком —
-- прокси работает под сервисным ключом и политик не касается.
--
-- ВАЖНО: выполнять ТОЛЬКО после того, как сервисный ключ появился на
-- хостинге — переменной SB_SERVICE_KEY либо файлом sb_service_key.php
-- рядом с db.php (см. sb_service_key.example.php). Иначе прокси останется
-- на анонимном ключе, и приложение ляжет целиком.
--
-- Что изменится для людей: перестанет работать realtime. Подписок четыре,
-- все ходят в базу напрямую и подчиняются политикам:
--   • новые сообщения в чате        (app/chat-room.tsx)
--   • смена статуса отклика         (app/chat-room.tsx)
--   • оценки в профиле и в чужом профиле
-- Первые две подстрахованы опросом раз в 8 секунд — сообщения продолжат
-- приходить, просто с задержкой до восьми секунд вместо мгновенной.
-- Оценки подтянутся при следующем открытии окна.
--
-- Загрузка фото и голосовых не пострадает: у Storage свои политики,
-- эта миграция их не трогает.
--
-- А вот дашборд ляжет: он ходит в таблицы напрямую анонимным ключом
-- (11 файлов). Это шаг 2 — перевести его на прокси. Пока шаг 2 не сделан,
-- дашборд после этой миграции работать не будет.

do $$
declare t text;
begin
  foreach t in array array[
    'jm_users', 'jm_vacancies', 'jm_perm_vacancies', 'jm_likes',
    'jm_perm_applications', 'jm_chats', 'jm_messages', 'jm_ratings',
    'jm_notifications', 'jm_complaints', 'jm_bulletins',
    'jm_saved', 'jm_perm_saved', 'jm_vacancy_views', 'jm_perm_vacancy_views',
    'jm_bulletin_views', 'jm_web_push_subscriptions', 'jm_worker_slots'
  ]
  loop
    if to_regclass('public.' || t) is not null then
      -- Включаем построчную защиту. Политик не создаём: без них доступ
      -- закрыт всем, кроме сервисного ключа — он RLS обходит.
      execute format('alter table public.%I enable row level security', t);
      execute format('revoke all on public.%I from anon', t);
      execute format('revoke all on public.%I from authenticated', t);
    end if;
  end loop;
end $$;

-- Проверить результат: с публичным ключом запрос ниже должен вернуть пусто
-- или ошибку доступа, а приложение — продолжить работать.
--   select id, phone from jm_users limit 1;
